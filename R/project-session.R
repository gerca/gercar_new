#DAY ONE 4 March 2019
#for notes

#object assigment
weight_kilos <- 100
weight_kilos

#vector
c("a", "b")
c(1, 2)
1:10

#data frame
head(iris)


colnames(iris)

#look at the structure
str(weight_kilos)
str(iris)

#summary statistics
summary(iris)


# Data wrangling (for example) --------------------------------------------
#In the code tab you can insert a section like the one above


# Object names
day_one

#T is existing name for true
T <- FALSE

#c is a function so not good to use it
c <- 9

#mean is also a function and is not descriptive
mean <- function(x) sum(x)

# Spacing, spacing after the comma
x[, 1]
x[, 1]
x[, 1]
# same space problem
mean (x, na.rm = TRUE)
mean(x, na.rm = TRUE)
function (x) {
}
function(x) {
}
height <- feet * 12 + inches
mean(x, na.rm = 10)
sqrt(x ^ 2 + y ^ 2)
# no spaces in this case
df$z
x <- 1:10

# Indenting, after if you need a tab or a curly bracket
if (y < 0 && debug)
  message("Y is negative")
#Using the code tab you can reformat code by selecting your code and it will fix some of the spacing and problems.


#with `` you see what the funtion does
`+`
`mean`

#lets create a function
summing(2, 2)


#with library you access packages
library(tidyverse)

usethis::use_r("package-loading")


#saving a file as a csv files, csv is universal
write_csv(iris, here::here("data/iris.csv"))


iris_data <- read_csv(here::here("data/iris.csv"))
iris_data


#stack overflow is for asking questions

#if you have questions you can do as follow:
?write.csv


#####ANNAS section Afternoon


